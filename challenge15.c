#include<stdio.h>
#include<math.h>
int main()
{
    int a,b;
    printf("Enter the values : ");
    scanf("%d%d",&a,&b);
    if(a>b)
    printf("a is greater");
    else
    printf("B is greater");
    return 0;
}